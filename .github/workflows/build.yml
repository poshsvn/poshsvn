on:
  push:
    branches:
      - "*"

jobs:
  build:
    runs-on: windows-latest
    env:
      Configuration: Release
  
    steps:
      - uses: actions/checkout@v3

      - name: Publish
        run: dotnet build .\PoshSvn\PoshSvn.csproj --output .\publish --configuration ${{ env.Configuration }}

      - name: Test
        run: dotnet test

      - name: Upload build artifact
        uses: actions/upload-artifact@v3
        with:
          name: PoshSvn
          path: .\publish
